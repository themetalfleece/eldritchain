# =============================================================================
# CreatureChain Indexer Environment Configuration
# =============================================================================
# Copy this file to .env and update the values for your environment

# =============================================================================
# SHARED CONFIGURATION (Required by both API and Indexer)
# =============================================================================

# MongoDB Configuration
# Required by: Both API and Indexer
# Description: MongoDB connection URI for storing indexed events
MONGODB_URI=mongodb://localhost:27017/eldritchain?replicaSet=rs0

# =============================================================================
# API SERVICE CONFIGURATION
# =============================================================================

# API Port
# Required by: API Service ONLY
# Description: Port number for the REST API server
PORT=3001

# =============================================================================
# INDEXER SERVICE CONFIGURATION
# =============================================================================

# Network Configuration
# Required by: Indexer Service ONLY
# Description: Blockchain network to connect to (polygonAmoy, polygon, sepolia, etc.)
NETWORK=polygonAmoy

# RPC URL Override
# Optional: Overrides the default public RPC URL for the selected network
# Description: Custom RPC endpoint (e.g. Alchemy, Infura, or your own node)
# RPC_URL=https://polygon-mainnet.g.alchemy.com/v2/YOUR_API_KEY

# Contract Address
# Required by: Indexer Service ONLY
# Description: Address of the deployed Eldritchain contract
CONTRACT_ADDRESS=0x1234567890abcdef1234567890abcdef12345678

# Start Block
# Required by: Indexer Service ONLY
# Description: Block number to start indexing from (use 0 for genesis)
START_BLOCK=12345

# Poll Interval
# Required by: Indexer Service ONLY
# Description: How often to poll for new blocks (in milliseconds)
# Recommended: 10000 (10 seconds) for most networks
POLL_INTERVAL=10000

# Optional
# Description: Safety buffer — how many blocks behind finalized to stay (handles reorgs/fluctuations)
# Default: 100 (if not specified)
# Recommendations:
#   - Ethereum Mainnet: 50 (finalized blocks very stable)
#   - Polygon Mainnet: 100 (good balance, handles fluctuations)
#   - Testnets: 200 (finality can fluctuate more)
SAFE_BLOCK_RANGE=100

# Optional
# Description: How many blocks to fetch per getLogs RPC call when catching up on historical blocks
# Default: 10 (if not specified)
# Higher values = faster catch-up but larger RPC responses - which could be out of range
BLOCK_CHUNK_SIZE=10

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================

# Replica Set Name (for MongoDB transactions)
# Optional: Used by Indexer Service ONLY (if using MongoDB transactions)
# Description: Name of the MongoDB replica set for transaction support
REPLICA_SET_NAME=rs0
