{
  "name": "eldritchain",
  "version": "1.0.0",
  "private": true,
  "description": "A decentralized daily creature summoning dApp with eldritch horror twist",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "web": "yarn workspace @eldritchain/web",
    "contracts": "yarn workspace @eldritchain/contracts",
    "indexer": "yarn workspace @eldritchain/indexer",
    "common": "yarn workspace @eldritchain/common",
    "lint": "yarn workspaces foreach -A run lint",
    "lint:fix": "yarn workspaces foreach -A run lint:fix",
    "format": "yarn workspaces foreach -A run format",
    "format:check": "yarn workspaces foreach -A run format:check"
  },
  "packageManager": "yarn@4.10.3",
  "devDependencies": {
    "prettier": "^3.0.0",
    "prettier-plugin-solidity": "^2.1.0"
  }
}
